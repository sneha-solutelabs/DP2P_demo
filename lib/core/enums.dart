enum APIErrorType {
  serverError,
  restrictedCountry,
  disableAccount,
  unwelcomeAccount,
  expiredAccount,
  unsupportedCurrency,
  notAvailableCountry,
  accountNotLoaded,
  unknownError,
}
enum AdvertType {
  buy,
  sell,
}

enum AdvertSortType {
  rate,
  completion,
}